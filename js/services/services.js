'use strict';
angular.module('conferenceroom.services', [])
/*
.factory('getLocationService', function($http, $q) {
    return {
        getLocation: function() {

         var deferred = $q.defer();

            $http.get('/conferencedata/location.json')
            .success(function(conferencedata) {
                deferred.resolve(conferencedata);
            })
            .error(function(reason) {
                deferred.reject(conferencedata);
            });
            return deferred.promise
        }
    };
})





.factory('getFacilityService', function($http, $q) {
    return {
        getFacility: function() {

         var deferred = $q.defer();

            $http.get('/conferencedata/facility.json')
            .success(function(conferencedata) {
                deferred.resolve(conferencedata);
            })
            .error(function(reason) {
                deferred.reject(conferencedata);
            });
            return deferred.promise
        }
    };
})*/
